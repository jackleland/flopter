image: python:3.6-alpine

pages:
  script:
  - pip install -r requirements.txt
  - apk --no-cache add make
  - cd docs/
  - make html
  - mv _build/html/ ../public/
  artifacts:
    paths:
    - public
  only:
  - master
